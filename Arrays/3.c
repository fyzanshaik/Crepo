#include <stdio.h>
#include <stdlib.h>

int main()
{
    int variable[10];
    //You can also make empty array and specify it's capacity to store and later add values in it
    variable[1] = 10;
    printf("%d", variable[1]);
    //Output is '10'

    return 0;
}