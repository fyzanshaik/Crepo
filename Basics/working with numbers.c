#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main()
{
    //Using functions that are pre built in C to do Math 
    printf("%f", pow(2, 3));
    //Exponent value of 2 to raised 3
    printf("\n%f", sqrt(49));
    // Square root of 49
    printf("\n%f", ceil(23.37));
    //This gives us the next highest number in whole number form , basically rounds off the decimal but still prints in float
    printf("\n%f", floor(23.37));
    //Does the opposite of "ceil"

    
}