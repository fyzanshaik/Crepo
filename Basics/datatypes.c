#include <stdio.h>
#include <stdlib.h>

int main()
{
    int age = 40;
    float price = 39.99;
    char letter = 'F';
    //To print a string use []
    char fullname[] = "Faizan";

    // printf("Age = %d\n", age);
    // printf("price = %d\n", price);
    // printf("letter = %s\n", letter);
    // printf("Fullname = %s\n", fullname);

    //Code won't run something to do with pointers and memory will look into it later/won't print gives no output 
    return 0;

}